services:
  - type: web
    name: freezer-manager
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES
        value: 30d
    healthCheckPath: /
    autoDeploy: true
